# Exemplo de variáveis de ambiente para a conexão com o banco
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=escola

# Dica: crie um arquivo .env na pasta backend e preencha com suas credenciais reais.
# Nunca compartilhe o arquivo .env em repositórios públicos.